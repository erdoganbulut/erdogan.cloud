---
import { getEntry } from "astro:content"

import SitePage from "@/components/site/SitePage.astro"

const entry = await getEntry("site", "en/home")
if (!entry) throw new Error("Missing English home content")
---

<SitePage entry={entry} alternatePath="/tr/" />
