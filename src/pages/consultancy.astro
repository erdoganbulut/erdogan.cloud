---
import { getEntry } from "astro:content"

import SitePage from "@/components/site/SitePage.astro"

const entry = await getEntry("site", "en/consultancy")
if (!entry) throw new Error("Missing English consultancy content")
---

<SitePage entry={entry} alternatePath="/tr/danismanlik/" />
